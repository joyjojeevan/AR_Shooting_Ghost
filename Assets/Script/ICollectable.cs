public interface ICollectable
{
    void Collect();
}
/*
 * It is just a contract.
       Any object that says ICollectable MUST have a Collect() function

It does NOT contain code.
It only forces structure.
 */